# Database Scripts

Use this directory for local database utility scripts that developers run manually.

## Current contents

- `seed-dev-data.js`: seed local development data
- `reset-local-database.js`: drop the local database

## Rule

Operational scripts belong here, not under `src/`.
