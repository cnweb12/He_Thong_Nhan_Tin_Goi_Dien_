# Middleware Layer

Use this directory for Express middleware such as:

- authentication and authorization
- request validation
- rate limiting
- logging and request tracing
- centralized error middleware when it grows beyond `app.js`

This is a valid architectural placeholder for the HTTP layer.
