# Shared Utils

Use this directory for generic utilities that are:

- not database infrastructure
- not HTTP middleware
- not specific to one module

If a helper is database-specific, keep it under `database/`.
